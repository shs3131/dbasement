{
  "servers": {
    "dbasement": {
      "type": "stdio",
      "command": "pwsh",
      "args": ["-ExecutionPolicy", "Bypass", "-File", "${workspaceFolder}/scripts/start.ps1", "--project", "${workspaceFolder}"]
    }
  }
}
